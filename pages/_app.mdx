import '../style.css'
import PlausibleProvider from "next-plausible";

export default function App({ Component, pageProps }) {
    return (
        <PlausibleProvider trackOutboundLinks trackFileDownloads domain="soulfiremc.com">
            <Component {...pageProps} />
        </PlausibleProvider>
    )
}
